apiVersion: v1
kind: Secret
metadata:
  name: oasis-secrets
type: Opaque
stringData:
  # In a real environment, DO NOT hardcode these here.
  # Use sealed secrets or an external secrets operator.
  JWT_SECRET_KEY: "dev-secret-change-me"
  GEMINI_API_KEY: "placeholder"
  POSTGRES_PASSWORD: "oasis_dev_password"
